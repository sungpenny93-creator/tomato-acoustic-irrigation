# ===========================================
# 樹莓派端套件清單（推論與控制用）
# 安裝方式：pip install -r requirements-pi.txt
# ===========================================

# TFLite 推論引擎（比完整 TensorFlow 小很多）
tflite-runtime>=2.13.0

# 即時音訊擷取
pyaudio>=0.2.13

# 數值運算
numpy>=1.24.0

# 訊號處理（頻譜減法）
scipy>=1.10.0

# GPIO 控制（Raspberry Pi 專用）
# RPi.GPIO>=0.7.1  # 取消註解以在樹莓派上安裝
